version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////data/app.db
      WEBHOOK_SECRET: ${WEBHOOK_SECRET}
      LOG_LEVEL: INFO
    volumes:
      - app_data:/data
